<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel='stylesheet' href='../styles/style.css' />
</head>
<body>
    <header class="bg-white py-6 px-12 flex justify-between items-center">
        <a href="/"><h1 class="font-bold text-3xl text-indigo-600">Expense Tracker</h1></a>
      <nav class="hidden">
        <% if(user !== null) { %>
            <h3 class="text-red-300 text-2xl font-bold mr-6"><%= user.name %></h3>
            <a href="/home" class="text-white text-xl font-bold mr-6">Dashboard</a>
            <a href="/users/logout" class="text-white text-xl font-bold">Logout</a>
          <% } else { %>
            <a href="/users/register" class="text-white text-xl font-bold mr-6">Register</a>
            <a href="/users/login" class="text-white text-xl font-bold">Login</a>
          <% } %>
      </nav>
    </header>

    <main>
        <section class="h-custom flex flex-col items-center w-1/3 mx-auto justify-center">
            <form action="/users/verifyEmail" method="POST" class="flex flex-col p-8 border-2 border-gray-300 rounded-lg w-custom">
                <% if(error) { %>
                    <legend class="text-red-500 font-bold"><%= error %></legend>
                <% } %>
                <input type="text" name="passcode" placeholder="Enter the Verification Code" class="p-3 bg-gray-200 my-2 rounded-lg border-2 border-gray-300">
                <input type="submit" value="Verify" class="p-3 bg-blue-500 text-white font-bold my-2 rounded-lg hover:bg-blue-400">
            </form>
        </section>
    </main>
    
</body>
</html>
